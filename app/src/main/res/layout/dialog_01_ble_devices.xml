<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="@drawable/list_dialog_background">

    <ImageView
        android:id="@+id/close_icon"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@drawable/ic_close_x"
        android:contentDescription="Fechar"
        android:layout_gravity="right"
        android:padding="8dp" />

    <TextView
        android:id="@+id/dialog_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="Dispositivos Bluetooth Disponíveis:"
        android:textColor="#fff"
        android:textSize="18sp" />

    <TextView
        android:id="@+id/dialog_title2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="Verifique se o dispositivo desejado está ligado e em modo de pareamento."
        android:textColor="#fff"
        android:textSize="14sp"
        android:layout_margin="16dp"/>


    <ProgressBar
        style="@android:style/Widget.Material.ProgressBar.Small"
        android:layout_width="25dp"
        android:layout_height="25dp"
        android:indeterminate="true"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="10dp"/>

    <ImageView
        android:layout_width="350dp"
        android:layout_height="wrap_content"
        android:background="@drawable/ic_auth_screen_separate_line"
        android:layout_margin="16dp"
        android:layout_gravity="center">

    </ImageView>

    <ListView
        android:id="@+id/dialog_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

</LinearLayout>